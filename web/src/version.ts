// src/version.ts
// Kept in sync manually with pyproject.toml's version and the README's
// Versions section — bumped together as part of each release's tagging step.
export const APP_VERSION = "0.2.1";
